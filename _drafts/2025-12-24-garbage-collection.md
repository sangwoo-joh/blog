---
layout: post
published: true
tag: [dev, ocaml, essay]
title: 가비지 컬렉션 이야기
---

모든 프로그램은 필연적으로 메모리를 사용해야 한다. 현대의 소프트웨어는 모두 폰 노이만 아키텍쳐 위에서 동작하기 때문에 데이터(와 코드)를 담을 공간이 필요하기 때문이다. 이로 인해서 모든 소프트웨어는 메모리 계층 구조의 한계를 벗어날 수 없기도 하다.

그래서 프로그램은 커널에게 부탁해서 (시스템 콜) 메모리를 얻는다. 그리고 다 사용한 메모리는 다시 커널에게 돌려줘야 한다. 메모리는 유한한 자원이기 때문이다. 안돌려주면 물리적인 메모리 크기의 한계 때문에 결국 커널은 패닉에 빠질 것이다.

그래서 보통은 프로그래머가 직접 메모리를 관리하는 코드를 작성한다. 커널에게 할당받아서 쓰고, 다 쓰고 나면 다시 커널에게 돌려준다. 이것이 기본이다. 그러나 프로그램이 복잡해지고 추가된 기능이 많아질수록, 메모리 관리 또한 복잡해진다. 그리고 사람은 실수를 저지른다. 커널에게 받은 메모리를 아직 쓰고 있는데 너무 빨리 돌려주거나 (Dangling Pointer / Use After Free), 까먹고 돌려주지 않거나 (Memory Leak), 같은 메모리를 여러 번 돌려주는 (Double Free) 등의 실수는 너무도 흔해서 각각에 이름 붙여질 정도다. 그리고 이들은 대부분 치명적인 오류를 일으켜서 프로그램을 죽여버린다. 아니면 적어도 프로그램의 성능의 발목을 잡는다.

그래서 자동으로 메모리를 관리하는 방법이 연구되기 시작한다. 이 방면의 유명 인사는 단연 자바일테지만, 최초의 자동 메모리 관리 방법은 1959년에 Lisp 프로그래밍 언어에 도입되었다. 인공지능 연구자 존 매카시 님의 [한 논문](https://www-formal.stanford.edu/jmc/recursive.pdf)의 27페이지에는 지금의 트레이싱 컬렉터(Tracing Collector)에 대한 기초적인 아이디어, 즉 *프로그램의 어떤 부분에서도 찾을 수 없는(닿을 수 없는) 데이터는 버려진 것으로 간주하고 나중에 재활용하는 방법*을 설명하고 있다.

> ... Such a register may be considered abandoned by the program because its contents can no longer be found by any possible program; hence its contents are no longer of interest, and so we would like to have it back on the free-storage list. ...

참고로 7번 주석에는 다음과 같은 재밌는 주석이 달려있는데:

> We already called this process "garbage collection", but I guess I chickened out of using it in the paper - or else the Research Laboratory of Electronics grammar ladies wouldn't let me.

이미 가비지 컬렉션이라는 최초로 이름을 붙여놓고 정작 논문에서는 쓰지 않다니, 존 매카시 님도 문법 교정 선생님들은 무서우셨나보다.

아무튼 이렇게 "필요할 때 메모리를 자동으로 커널로부터 할당하고, 다 쓴 메모리는 자동으로 커널에게 돌려주는" 방법, 즉 가비지 컬렉션에는 크게 두 가지 갈래가 있다.

# Reference Counting
한 가지 방법은 레퍼런스 카운팅이다. 모든 메모리를 프로그램에서 사용하는 어떤 표준적인 객체로 추상화한 다음, 이 객체가 프로그램의 다른 곳에서 얼마나 참조되는지 (레퍼런스) 그 횟수를 세어서 (카운팅), 어떤 객체가 가비지인지 여부를 자동으로 판단하는 방법이다. 처음 객체가 만들어지면 당연히 참조 횟수가 1이 될테고, 이후 이 객체가 다른 곳에서 쓰이거나 하면 횟수가 늘어난다. 그리고 객체를 쓰던 함수가 리턴하여 할 일을 끝내거나 하면 횟수가 줄어든다. 그러다가 이 참조 횟수가 0이 되면 이는 곧 이 객체를 참조하는 곳이 아무도 없다는 의미이므로 이 객체와 관련된 메모리를 모두 수집하는 방법이다.

레퍼런스 카운팅은 여러가지 장점과 단점이 있다. 장점으로는 메모리(객체)를 다 쓴 *즉시* 수집되기 때문에 객체의 수명이 명확해서 메모리를 덜 사용하고 일반적으로는 프로그램의 응답 시간(Latency)이 짧다는 점이 있다. 그리고 생각보다 구현도 간단하다...고 하는데, CPython 구현 코드에서 레퍼런스 카운터를 [늘리거나](https://github.com/search?q=repo%3Apython%2Fcpython%20py_incref&type=code) [줄이는](https://github.com/search?q=repo%3Apython%2Fcpython+py_decref&type=code) 코드를 검색하면 엄청 많은 곳에서 발견되어서, 그렇게 쉬울 거라는 생각은 들지 않는다.

반면 단점으로는 먼저 가장 유명한 순환 참조, 이른바 싸이클이 있다. 두 객체가 서로 참조해버리면 이 객체(들)을 사용하던 곳이 일을 다하고 소멸되어도 여전히 참조 횟수가 1 이상이라서 절대로 수집되지 않는다 (Memory Leak). 그래서 파이썬과 같은 레퍼런스 카운팅 기반 가비지 컬렉션 언어에서는 추가적으로 (아래에서 설명할) 트레이싱 컬렉터도 같이 도입해서 이런 가비지를 따로 수집하도록 하기도 한다. 또 다른 단점으로, 엄청나게 많은 수의 객체의 참조가 한꺼번에 0이 되어 가비지가 되어 버리면 이들을 한꺼번에 수집하느라 큰 오버헤드가 발생하게 되고 이로 인해서 성능이 튈 수 있다 (Jitter). 이를 위해 느린 해제 (Lazy Freeing) 같은 다양한 방법이 연구되기도 했다.

사실 개인적으로 생각하기에 더 큰 단점은 바로 모든 객체에 추가적인 카운터가 붙고, 이로 인해서 **객체를 읽는 모든 연산이 쓰기 연산이 되어버린다**는 점이다. 이로 인해서 데이터를 단순히 읽기만 하는 것이 원천적으로 불가능하다. 예를 들어 거대한 데이터를 여러 프로세스/쓰레드 사이에 읽기만 하는 목적으로 공유하는 경우, 커널이 제공하는 최적화 중 하나인 쓰기 시 복사(Copy-on-Write)를 적용하는 것이 불가능해져서 예상했던 것보다 엄청나게 많은 메모리를 잡아먹게 된다. 그래서 병렬 처리가 불가능했던 적이 있다. 이럴 때는 파이썬 자체에서 데이터를 로딩하기 보다는 다른 라이브러리를 이용해서 읽기만 할 데이터를 공유 메모리에 올리는 방법으로 우회해야 한다. 최적화를 못받는다는 점 외에도, "읽기가 곧 쓰기"가 되는 특성으로 인해서 공유 메모리 병렬 처리 (Shared Memory Parallelism), 다시 말해서 멀티 쓰레드 환경에서의 런타임의 구현이 어려워진다. 여러 개의 프로세스/쓰레드가 공유된 오브젝트를 "읽기"만 해도 카운터를 "쓰게" 되는데, 이는 곧 레이스 컨디션을 의미하고 그러면 반드시 동기화가 필요하기 때문이다. 그래서 이를 간단하게 하기 위해서 파이썬은 이른바 GIL(Global Interpreter Lock)을 도입하기도 했다.

# Tracing Collection
가비지 컬렉션의 다른 방법으로는, 원조 논문인 LISP에서도 적용한 바로 그 방법이자 여기서 주로 얘기할 **트레이싱 컬렉션(Tracing Collection)**이 있다. 이 방법의 핵심은, 프로그램의 어떤 실행 시점에 사용 중인 객체들(루트)로부터 닿을 수 있는 (Reachable) 모든 객체들은 가비지가 아니기 때문에, 나머지 닿을 수 없는 (Unreachable) 객체들을 모두 가비지로 간주하고 수집하는 것이다. 이 문장만 읽어도 알 수 있겠지만 이 방법을 구현하는 것은 그리 간단해보이지 않는다. 그래서 정말 다양한 방법들이 연구되어 왔고 지금도 개선되고 있다. 그리고 내가 가장 흥미를 느끼는 가비지 컬렉션 방법이기도 하다. 여기서는 트레이싱 컬렉션과 관련해서 주절주절 아무 이야기나 해보려고 한다. 특히 최근에 내가 개인적으로 좋아하는 언어인 OCaml 소스 코드를 살펴보면서 얻은 현인들의 지혜 같은 것들을 좀 정리해두고자 한다.

## Generational Hypothesis
세대 가설 - 대부분의 데이터는 빨리 죽는다 (Die Young). 이로 인해서 말 그대로 여러 개의 힙을 운영하는 것이 가능해진다. 그래서 보통은 두 개의 힙을 운영한다.

### Minor Heap
Youngest Heap, Bump Pointer Collection

### Major Heap
Old Heap, Mark & Sweep Collection

## STW (Stop-The-World) Coordination
결국 트레이싱 컬렉션도 색깔을 칠하는 작업을 해야하기 때문에 오브젝트에 값을 써야하는데, 이로 인해 레퍼런스 카운팅과 마찬가지로 멀티 프로세스/쓰레드 환경에서 동기화가 필수적이다. 그래서 OCaml도 파이썬과 마찬가지로 GIL이 존재**했다**.

### Snapshot Invariant
마크 앤 스윕의 알고리즘 특성 상, **가비지 (즉 Unreachable Objects)의 수는 절대로 줄어들지 않는다**. 그래서 컬렉션 (정확히는 마킹) 을 시작하는 순간 현재 오브젝트들의 상태의 스냅샷(즉 주소 값들)을 복사해서 가지고 있으면서, 얘네들을 따라가서 가비지인지 여부를 확인한다. 이렇게 하면 **반드시 마킹 작업이 끝난다**는 것을 보장할 수 있는데, 컬렉션이 동작하는 도중에 힙에 오브젝트들이 계속 생기면, 이로 인해서 컬렉션이 늘어지거나 끝나지 않을 수 있기 때문이다. 그래서 보통은 이렇게 컬렉션이 시작하는 시점의 스냅샷만을 기준으로 GC가 동작한다.

## Incremental
responsiveness, 반응성이 중요.
전체 다 한꺼번에 하는게 아니라, 마킹이랑 스위핑 페이즈를 일단 나누고, 각 단계를 잘라서 (Slicing) 조금 씩 진행해서 (Incremental) 사용자 프로그램이 너무 긴 정지 시간 (Pause Time)을 겪지 않고 반응성이 좋도록 한다.

## Ephemeron, Finalizer
## Free List
다 사용한 메모리 오브젝트를 따로 관리하다가 나중에 다시 사용하는 것은 매우 직관적인 개념이다. 다 쓴 메모리 블록을 단순히 리스트로 관리하기만 하면 된다. 그래서 초창기부터 가장 널리 쓰인 구현은 이런 다 쓴 오브젝트를 관리하는 Free List이다.

## Write Barrier
## Locality
## Memory Representation

### Type System

---

여기까지 얘기한 모든 GC는 **싱글 쓰레드/프로세스** 기준이다. 트레이싱 컬렉션을 확장해서 병렬/동시성/멀티코어 컬렉션으로 최적화하려는 시도는 많은 언어에서 있어왔다. 대표적으로 자바는 다양한 알고리즘으로 구현된 GC를 선택할 수 있고, 하스켈은 아예 싱글 쓰레드 런타임과 동시성(병렬) 런타임이 구분되어 있다. 

OCaml에서는 5.0 버전부터 이러한 동시성/병렬 컬렉션이 추가되면서 진정한 의미의 병렬 처리가 가능해졌다. 큰 특징 중 하나는 **하나의 런타임**을 강제한 것으로, 기존의 5.0 미만 버전에서 동작하던 **모든** 코드들이 이 새로운 런타임과 (약간의 오버헤드를 희생하여) 호환되도록 구현한 것이 특징이다. 이를 위해서 거의 10년에 걸쳐 수많은 연구 개발 시도가 있었는데, 특히 [KC Sivaramakrishnan](https://kcsrk.info/) 교수님과 [OCaml Labs](https://ocamllabs.io/) (지금은 Tarides에 통합됨) 에서 ICFP, PLDI 등 탑티어 PL 학회에 연구 결과를 논문으로 출판했다. 하지만, OCaml의 가장 강력한 사용자 중 하나인 제인 스트리트에서 이를 프로덕션에 적용해본 결과, 논문에서 주장했던 3% 정도의 오버헤드가 아니라 생각보다 더 큰 (10% 이상) 오버헤드 및 메모리 사용량의 증가가 있었다. 그래서 이 이슈를 더 깊게 파고 들어서 어떤 문제가 있는지 정확하게 파악하고, 이를 수정하고, 보완하고, 가능한 최적화를 추가하는 등 정말로 프로덕션에서 써먹을 수 있을 정도의 개선을 하는데 또 거의 3년이 걸렸고, 그 결과 이제 정말 최신 버전의 OCaml 5.3 에서는 

# Parallel Tracing Collection
## Multi Domain Architecture
## Parallel Marking
## Memory Pool
사실 초창기에는 메모리가 극도로 제한적이었다. 그래서 자동 메모리 관리라는 개념 자체를 작동시키는 구현을 하기 위해서는 현대적인 복잡한 메모리 관리 방법을 도입할 여력이 없었던 게 아닐까?

하지만 이제는 아니다. 누구나 수 기가, 수십 기가, 원한다면 수백 기가의 메모리를 적절한 가격에 사서 개인용 PC를 굴릴 수 있는 하드웨어의 시대가 열렸다.

## Concurrent Sweeping
## Optimisation
## Parallel Processing
